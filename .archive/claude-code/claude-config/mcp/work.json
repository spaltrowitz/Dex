{
  "name": "work",
  "description": "Work management - quarterly goals, weekly priorities, and daily tasks with pillar alignment",
  "server": {
    "command": "python",
    "args": ["core/mcp/work_server.py"],
    "env": {
      "VAULT_PATH": "${workspaceFolder}"
    }
  },
  "notes": "Manages complete planning hierarchy: 01-Quarter_Goals/Quarter_Goals.md → Week Priorities.md → 03-Tasks/Tasks.md. Loads pillars from System/pillars.yaml."
}
